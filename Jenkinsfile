

pipeline {
  agent: any

  stages {
	stage('build') {
            echo 'build'
         }

	stage('upload') {
	   echo 'upload'
	}

        stage('test') {
          echo 'test'
        }
	stage('deploy') {
	 echo 'bug'
	 echo 'deploy''
        }
  }

}
